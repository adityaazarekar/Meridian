VITE_API_BASE=http://localhost:5000
# Optional: logo.dev token for https://img.logo.dev/ticker/{SYM}.png (embedded in client bundle — use a restricted key or proxy in production)
VITE_LOGO_DEV_TOKEN=
